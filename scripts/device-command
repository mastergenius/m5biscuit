#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "$0")/.." && pwd)"
python_bin="$repo_root/.pio/core/penv/bin/python"

if [[ ! -x "$python_bin" ]]; then
  python_bin="${PYTHON:-python3}"
fi

exec "$python_bin" "$repo_root/scripts/device_command.py" "$@"
