# Worklog 2026-05-20

## Итоги дня

- Стабилизирован текущий M5Paper path: чтение, StudyPacks, File Transfer, deep sleep/wake и
  greyscale-рендер доведены до рабочего состояния для ручной проверки.
- Поднята offline-first линия StudyPacks: курс-источники остаются в markdown/YAML-дереве, а
  disposable personalized StudyPacks генерируются как расходуемые учебные юниты.
- Запущен host-side Device Sync: локальный FastAPI sync host, HMAC challenge flow, temporary
  bootstrap pairing, локальный ignored firmware fallback и per-device secret на SD.
- Перевели host tooling на `uv`: зависимости живут в `agents/device-sync/.venv`, cache в
  `agents/device-sync/.uv-cache`, запуск работает через `agents/device-sync/run`,
  `dev-server`, `dev-tmux`.
- Проверен end-to-end путь: host в tmux, `/health`, `/api/v0/host`, bootstrap/HMAC sync, затем
  реальный M5Paper показал `Sync complete`.

## Ночной план

1. Улучшить результат Device Sync на устройстве: показывать не только `Sync complete`, а конкретно
   что отправлено, что принято host-side, сколько notes/study logs/status acked, и понятный hint
   для выхода.
2. Расширить host response без ломки firmware API: добавить summary/counts поверх текущего `acked`,
   чтобы следующая итерация могла принимать StudyPacks и другие payload classes.
3. Добавить воспроизводимый smoke-test для FastAPI sync host через локальный `uv` wrapper.
4. Документировать утренний тест: что запустить в tmux, что нажать на M5Paper, какие endpoints/logs
   проверить.
5. Собрать `m5paper` и `default`; если `default` всё ещё около лимита flash, явно отметить риск.

## Ночная Реализация

- Device Sync UI теперь показывает результат синхронизации: status ack, количество принятых notes,
  количество study logs, количество полученных StudyPacks и факт сохранения pairing на SD.
- Host `/api/v0/host` объявляет `sync-summary-v0`.
- Host `/api/v0/sync` оставляет старое поле `acked`, но дополнительно возвращает:
  - `received` для будущих входящих payloads;
  - `summary.accepted` с count-ами по status/notes/study_logs;
  - `summary.received` для device-side UI.
- Добавлен `agents/device-sync/smoke-test`: временный host root, bootstrap pairing, normal HMAC sync,
  проверка `sync-summary-v0`, status, note и study log ack.
- Проверки:
  - `agents/device-sync/smoke-test`: pass;
  - Python compile для device-sync scripts: pass;
  - `pio run -e m5paper`: pass, flash 3,578,939 / 6,553,600 bytes;
  - `pio run -e default`: pass, flash 6,549,901 / 6,553,600 bytes.

## Утренний Чеклист

1. Убедиться, что host всё ещё жив:

   ```bash
   curl http://127.0.0.1:8787/api/v0/host
   ```

   В `capabilities` должно быть `sync-summary-v0`.

2. Залить свежую прошивку:

   ```bash
   pio run -e m5paper -t upload --upload-port /dev/cu.usbserial-5B1F0123571
   ```

3. На M5Paper открыть `Device Sync`.
4. Ожидаемый экран: `Sync complete`, `Sent: status Accepted: yes`, `Notes: 0 Study logs: 0`,
   `Received packs: 0`.
5. В tmux проверить, что request пришёл от M5Paper и `payload_keys` содержит `status`:

   ```bash
   tmux capture-pane -t biscuit-sync -p | tail -80
   ```

## Backlog После Ночи

- Доставка StudyPacks обратно на устройство через sync, не через File Transfer.
- Выгрузка реальных review logs с устройства в `agents/device-sync/inbox/study-logs`.
- Secret rotation и QR/on-device pairing вместо embedded debug fallback.
- Убрать `task_wdt` spam из старых логов и проверить watchdog policy.
- Улучшить greyscale/menu ghosting при выходе из reader/menu.
